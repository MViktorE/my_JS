setInterval(() => {
	document.querySelectorAll('.mp_tour').forEach(item => {
		item.closest('.lobby-item').querySelector('.lobby-item__green')?.click();
	document.getElementById('react-confirm-alert')?.querySelector('.green').click();
	})
}, 100)